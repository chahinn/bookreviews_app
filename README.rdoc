Rails app with several popular books and reviews written by readers, many to many database model.

The app uses route function to create a new site when a book is clicked. The side displays all the reviews written about that book.
